---
id: TASK-2026-04-04-019
title: Implement clipboard copy with 45-second auto-clear
domain: rust-backend
goal: >
  Create a Tauri command `copy_to_clipboard` that copies a value to the system
  clipboard and automatically clears it after 45 seconds. The timer resets if
  a new copy is triggered before expiry.
scope_in:
  - src-tauri/src/commands/
  - src-tauri/src/clipboard/
  - src-tauri/Cargo.toml
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Use Tauri's clipboard API or `arboard` crate.
  - Auto-clear after 45 seconds — only clear if the clipboard still contains the value we set.
  - Timer must reset on subsequent copies.
  - Zeroize the value in memory after setting clipboard.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
output:
  - List changed files.
  - Document the clipboard command.
estimate_minutes: 20
dependencies:
  - TASK-2026-04-04-001
design_revision: false
collaborators: []
status: approved
---

## Context
Phase 2 — Desktop Shell. PRD section 4.3 requires "Copy password to clipboard with an
automatic 45-second clear timer." This is a security-critical feature.

## Completion Criteria
- `copy_to_clipboard` sets clipboard and schedules auto-clear.
- Auto-clear fires after 45 seconds.
- Clipboard is only cleared if it still holds the value we set.
- Build succeeds.
