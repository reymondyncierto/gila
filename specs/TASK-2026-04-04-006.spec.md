---
id: TASK-2026-04-04-006
title: Implement database CRUD operations for credential entries
domain: database
goal: >
  Implement Rust functions for creating, reading, updating, and deleting credential
  entries in the SQLite database. These functions operate on encrypted data — they
  store and retrieve ciphertext blobs without performing encryption themselves.
scope_in:
  - src-tauri/src/db/
scope_out:
  - src/
  - src-tauri/src/crypto/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Functions must handle all five credential types.
  - Support listing entries with filtering by type and favorites.
  - Support search by querying decrypted metadata (service name, username) — this will require the caller to pass search terms; the DB layer stores an optional plaintext index field for searchable metadata.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
  - cd src-tauri && cargo test
output:
  - List changed files.
  - Summarize the CRUD API surface.
estimate_minutes: 30
dependencies:
  - TASK-2026-04-04-005
design_revision: false
collaborators: []
status: approved
---

## Context
Phase 2 — Desktop Shell. This builds on the schema from TASK-005 to provide the data
access layer. The Tauri commands (TASK-007, TASK-008) will call these functions.

## Completion Criteria
- CRUD operations for all credential types work correctly.
- List with type filtering works.
- Favorite toggling works.
- Unit tests pass.
