---
id: TASK-2026-04-04-012
title: Build credential list view with category filtering
domain: ui-ux-frontend
goal: >
  Create the credential list component that displays vault entries in the sidebar
  or main panel. Each entry shows service name, username, and credential type icon.
  Clicking a category in the sidebar filters the list.
scope_in:
  - src/components/credentials/
  - src/hooks/
  - src/types/
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - TypeScript types for all five credential types.
  - Call `list_credentials` Tauri command to fetch entries.
  - Filter by credential type when a sidebar category is selected.
  - Show empty state when no credentials exist.
  - Passwords must NOT be visible in the list view.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the component hierarchy.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-009
  - TASK-2026-04-04-008
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. The list view is the primary navigation interface for the vault.
It consumes the `list_credentials` Tauri command from TASK-008.

## Completion Criteria
- Credential list renders with name, username, and type icon.
- Category filtering works.
- Empty state is shown.
- No passwords visible.
- Build succeeds.
