---
id: TASK-2026-04-04-015
title: Build edit and delete credential functionality
domain: ui-ux-frontend
goal: >
  Add edit and delete capabilities to the credential detail view. Edit opens a pre-filled
  form; delete shows a confirmation dialog. Both call the corresponding Tauri commands.
scope_in:
  - src/components/credentials/
  - src/components/forms/
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Edit form reuses the create form component with pre-filled values.
  - Delete requires a confirmation dialog before proceeding.
  - Call `update_credential` and `delete_credential` Tauri commands.
  - Refresh the credential list after edit or delete.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the edit/delete UX flow.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-014
  - TASK-2026-04-04-013
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. Completes the CRUD cycle for credentials in the frontend.
Auth-gating for edit/delete will be layered on in Phase 3.

## Completion Criteria
- Edit button opens pre-filled form.
- Delete shows confirmation dialog.
- Both call Tauri commands.
- List refreshes after mutation.
- Build succeeds.
