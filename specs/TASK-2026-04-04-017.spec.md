---
id: TASK-2026-04-04-017
title: Implement password generator backend (Rust)
domain: vault-domain
goal: >
  Create a Rust module for generating secure random passwords and passphrases.
  Expose configurable options: length, character sets (uppercase, lowercase, digits,
  symbols), and passphrase mode (word-based with configurable separator and word count).
scope_in:
  - src-tauri/src/generator/
  - src-tauri/src/commands/
  - src-tauri/src/lib.rs
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Use CSPRNG (`rand` crate) for all random generation.
  - Character mode: configurable length (8-128), toggleable character sets.
  - Passphrase mode: configurable word count (3-10), word separator, use an embedded word list.
  - Expose as Tauri command `generate_password`.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
  - cd src-tauri && cargo test
output:
  - List changed files.
  - Document the generator API and options.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-001
design_revision: false
collaborators: []
status: approved
---

## Context
Phase 2 — Desktop Shell. The password generator is a core vault feature (PRD section 4.3).
This spec covers the Rust backend; the frontend UI is in TASK-018.

## Completion Criteria
- Character-mode generator produces passwords of specified length with correct character sets.
- Passphrase-mode generator produces word-based passwords.
- Tauri command is registered.
- Unit tests pass.
