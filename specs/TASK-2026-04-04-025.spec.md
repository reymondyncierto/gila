---
id: TASK-2026-04-04-025
title: Integrate desktop biometric authentication (Windows Hello / Linux fprintd)
domain: auth-domain
goal: >
  Integrate `tauri-plugin-biometric` for desktop biometric authentication. Support
  Windows Hello and Linux PAM/fprintd. Biometric unlock stores a derived session key
  in the OS Secure Enclave. Wire biometric auth into the lock screen and per-action
  auth prompt.
scope_in:
  - src-tauri/src/auth/
  - src-tauri/src/commands/
  - src-tauri/Cargo.toml
  - src/components/auth/
  - src/pages/LockScreen.tsx
scope_out:
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Use `tauri-plugin-biometric` for biometric prompts.
  - Gracefully degrade if biometrics are unavailable (fall back to master password).
  - On first biometric enrollment, derive a session key and store it in the OS keychain.
  - On biometric auth, retrieve the session key to unlock the vault.
  - Wire into both the lock screen and the auth prompt modal.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
  - npm run build
output:
  - List changed files.
  - Document the biometric flow.
  - Note platform-specific behavior.
estimate_minutes: 30
dependencies:
  - TASK-2026-04-04-023
  - TASK-2026-04-04-024
design_revision: false
collaborators:
  - rust-backend-specialist
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 3 — Biometric Gate. This is the capstone of Phase 3, bringing biometric
authentication to the desktop app. The "Talon Bridge" from the PRD.

## Completion Criteria
- Biometric prompt appears on supported systems.
- Fallback to master password on unsupported systems.
- Session key is stored in OS keychain.
- Biometric unlock works from lock screen and auth modal.
- Both builds succeed.
