---
id: TASK-2026-04-04-007
title: Implement Tauri commands for vault write operations (create, update, delete)
domain: rust-backend
goal: >
  Create Tauri invoke commands that the frontend can call to create, update, and delete
  credential entries. These commands orchestrate the crypto module (encrypt data) and
  the database module (persist encrypted data).
scope_in:
  - src-tauri/src/commands/
  - src-tauri/src/lib.rs
  - src-tauri/src/main.rs
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Each command must accept typed input matching the credential type schemas.
  - Encrypt all sensitive fields before passing to the DB layer.
  - Zeroize plaintext after encryption.
  - Register commands in the Tauri app builder.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
output:
  - List changed files.
  - Document the Tauri command signatures.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-004
  - TASK-2026-04-04-006
design_revision: false
collaborators:
  - crypto-domain-specialist
  - database-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. These commands form the write-side API that the React frontend
invokes. They bridge the frontend to the crypto + database layers.

## Completion Criteria
- `create_credential`, `update_credential`, `delete_credential` Tauri commands exist.
- Commands encrypt data before storage.
- Commands are registered and accessible from the frontend via `invoke`.
