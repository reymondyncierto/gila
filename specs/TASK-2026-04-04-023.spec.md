---
id: TASK-2026-04-04-023
title: Build lock screen UI
domain: ui-ux-frontend
goal: >
  Create the lock screen that appears when the vault is locked. Shows the app logo,
  a master password input, and a biometric button (placeholder for Phase 3 biometric
  integration). On successful unlock, transitions to the main vault view.
scope_in:
  - src/pages/LockScreen.tsx
  - src/components/auth/
  - src/App.tsx
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Centered layout with app branding.
  - Master password input with show/hide toggle.
  - Submit calls `unlock_vault` Tauri command.
  - Show error on incorrect password.
  - Biometric button (non-functional placeholder for now).
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the lock screen layout.
estimate_minutes: 20
dependencies:
  - TASK-2026-04-04-021
  - TASK-2026-04-04-009
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 3 — Biometric Gate. The lock screen is the first thing users see when the app is
locked. It consumes the `unlock_vault` command from TASK-021.

## Completion Criteria
- Lock screen renders with password input and biometric placeholder.
- Incorrect password shows error.
- Successful unlock transitions to vault view.
- Build succeeds.
