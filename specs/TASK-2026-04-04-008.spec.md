---
id: TASK-2026-04-04-008
title: Implement Tauri commands for vault read operations (list, get, search)
domain: rust-backend
goal: >
  Create Tauri invoke commands for listing credentials, getting a single credential's
  details (decrypted), and searching credentials. The list command returns metadata
  only (no decrypted secrets). The get command decrypts and returns full details.
scope_in:
  - src-tauri/src/commands/
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - `list_credentials` returns metadata (name, type, username, date) without decrypting secrets.
  - `get_credential` decrypts and returns full credential data, including sensitive fields.
  - `search_credentials` performs fuzzy matching on the plaintext index fields.
  - Zeroize decrypted data appropriately.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
output:
  - List changed files.
  - Document the Tauri command signatures.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-007
design_revision: false
collaborators:
  - crypto-domain-specialist
  - database-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. These commands form the read-side API. The list command powers
the credential list view; the get command powers the detail view after auth-gated reveal.

## Completion Criteria
- `list_credentials`, `get_credential`, `search_credentials` Tauri commands exist.
- List returns metadata without secrets.
- Get decrypts and returns full data.
- Search filters by name/service.
