---
id: TASK-2026-04-04-020
title: Implement dark/light theme support with system detection
domain: ui-ux-frontend
goal: >
  Add system-native dark and light mode support. Detect the OS theme preference
  and apply the corresponding Tailwind theme. Allow manual toggle as well.
scope_in:
  - src/index.css
  - src/hooks/
  - src/components/layout/
  - tailwind.config.js
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Use Tailwind's `dark:` variant with the `class` strategy for flexibility.
  - Detect system preference via `prefers-color-scheme` media query.
  - Provide a theme toggle in the UI (e.g., in the sidebar footer or settings).
  - Persist theme preference in localStorage.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe how theming works.
estimate_minutes: 20
dependencies:
  - TASK-2026-04-04-009
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. PRD section 5 requires "System-native Dark/Light mode support."

## Completion Criteria
- Dark and light themes render correctly.
- System preference is auto-detected.
- Manual toggle persists across sessions.
- Build succeeds.
