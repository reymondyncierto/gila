---
id: TASK-2026-04-04-010
title: Build onboarding flow for master password creation
domain: ui-ux-frontend
goal: >
  Create the onboarding screen where a first-time user sets their Master Password.
  The screen collects the password, confirms it, and invokes the Tauri command to
  initialize the vault (generate salt, derive key, store verification token).
scope_in:
  - src/pages/Onboarding.tsx
  - src/components/onboarding/
  - src/App.tsx
scope_out:
  - src-tauri/src/crypto/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Password strength indicator (visual feedback).
  - Confirm password field with match validation.
  - Call Tauri `invoke` to initialize the vault on submit.
  - Show loading state during vault initialization.
  - Route to main app after successful setup.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the onboarding UX flow.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-009
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. First-run experience. The Tauri backend command for vault
initialization (TASK-007 dependency) must exist, but the frontend can be built with
a mock invoke if needed.

## Completion Criteria
- Onboarding screen renders with password + confirm fields.
- Strength indicator shows feedback.
- Submit calls Tauri invoke.
- Build succeeds.
