---
id: TASK-2026-04-04-004
title: Create crypto integration tests and end-to-end encrypt/decrypt pipeline
domain: crypto-domain
goal: >
  Write integration tests that exercise the full crypto pipeline: password -> Argon2id
  key derivation -> AES-GCM encryption -> decryption -> verify plaintext. Ensure the
  crypto module's public API is ergonomic for vault operations.
scope_in:
  - src-tauri/src/crypto/
  - src-tauri/tests/
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Tests must cover: full roundtrip, wrong password fails decryption, corrupted ciphertext fails, empty plaintext, large plaintext.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo test
output:
  - List changed files.
  - Summarize test coverage.
estimate_minutes: 20
dependencies:
  - TASK-2026-04-04-003
design_revision: false
collaborators: []
status: approved
---

## Context
Phase 1 — Core Crypto. Final task in Phase 1. These integration tests validate that the
Argon2id + AES-GCM pipeline works correctly end-to-end, completing the "Rust CLI that
encrypts/decrypts" deliverable from the PRD roadmap.

## Completion Criteria
- All integration tests pass.
- Wrong password correctly fails to decrypt.
- Corrupted ciphertext is detected and rejected.
