---
id: TASK-2026-04-04-021
title: Implement auth state management with auto-lock on inactivity and sleep
domain: auth-domain
goal: >
  Create a Rust-side auth state manager that tracks whether the vault is locked or
  unlocked. Auto-lock the vault after 5 minutes of inactivity or on system sleep.
  Expose Tauri commands to query and change the lock state.
scope_in:
  - src-tauri/src/auth/
  - src-tauri/src/commands/
  - src-tauri/src/lib.rs
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Track an in-memory auth state (locked/unlocked) with a last-activity timestamp.
  - Auto-lock after 5 minutes of inactivity (configurable constant).
  - Listen to system sleep events (Tauri window events) to trigger lock.
  - Expose `get_lock_state`, `lock_vault`, `unlock_vault` commands.
  - `unlock_vault` must verify the master password before transitioning to unlocked.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
output:
  - List changed files.
  - Document the auth state machine.
estimate_minutes: 30
dependencies:
  - TASK-2026-04-04-011
design_revision: false
collaborators: []
status: approved
---

## Context
Phase 3 — Biometric Gate. This is the core auth state management that all auth-gated
operations depend on. The lock/unlock state determines whether the app shows the lock
screen or the vault.

## Completion Criteria
- Auth state tracks locked/unlocked correctly.
- Auto-lock triggers after 5 minutes of inactivity.
- System sleep triggers lock.
- Tauri commands work correctly.
- Build succeeds.
