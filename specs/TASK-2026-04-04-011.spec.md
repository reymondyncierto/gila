---
id: TASK-2026-04-04-011
title: Implement Tauri command for vault initialization (master password setup)
domain: rust-backend
goal: >
  Create a Tauri command `initialize_vault` that accepts a master password, generates
  a salt, derives the encryption key via Argon2id, encrypts a known verification token,
  and stores the salt + verification ciphertext in the database. Also create a
  `verify_master_password` command for unlock.
scope_in:
  - src-tauri/src/commands/
  - src-tauri/src/lib.rs
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - `initialize_vault` must fail if vault is already initialized.
  - `verify_master_password` must derive the key and attempt to decrypt the verification token.
  - Store salt in the database, not in a config file.
  - Zeroize the master password and derived key after use.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
output:
  - List changed files.
  - Document the command signatures.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-004
  - TASK-2026-04-04-005
design_revision: false
collaborators:
  - crypto-domain-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. This command is called by the onboarding flow (TASK-010) and
the lock screen (TASK-019). It bridges the crypto module and the database for vault
lifecycle management.

## Completion Criteria
- `initialize_vault` creates salt + verification token in DB.
- `verify_master_password` correctly validates the password.
- Wrong password is rejected.
- Double initialization is rejected.
