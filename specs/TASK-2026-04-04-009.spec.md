---
id: TASK-2026-04-04-009
title: Build app layout shell with sidebar and detail panel (glassmorphism)
domain: ui-ux-frontend
goal: >
  Create the main application layout with a sidebar for categories (All, Logins,
  App Passwords, API Keys, Wi-Fi, Secure Notes, Favorites) and a detail panel.
  Apply a glassmorphism / macOS-inspired aesthetic using Tailwind CSS.
scope_in:
  - src/App.tsx
  - src/components/layout/
  - src/index.css
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Use React 19 functional components with TypeScript.
  - Use Tailwind CSS for all styling — no CSS-in-JS or CSS modules.
  - Sidebar must list all credential categories with icons.
  - Layout must be responsive within the desktop window.
  - Glassmorphism effect: frosted glass backgrounds, subtle blur, translucency.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the layout structure.
estimate_minutes: 30
dependencies:
  - TASK-2026-04-04-001
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. This is the foundational UI component. All other frontend
specs build on top of this layout shell.

## Completion Criteria
- Sidebar renders all categories.
- Detail panel area renders as an empty placeholder.
- Glassmorphism styling is applied.
- Build succeeds.
