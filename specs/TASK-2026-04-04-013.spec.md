---
id: TASK-2026-04-04-013
title: Build credential detail view with masked password display
domain: ui-ux-frontend
goal: >
  Create the detail view component that shows a credential's full information when
  selected. Sensitive fields (passwords, keys, secrets) are masked with dots.
  A "Reveal" button placeholder is present but auth-gating is handled in Phase 3.
scope_in:
  - src/components/credentials/
  - src/hooks/
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Display all fields appropriate to the credential type.
  - Sensitive fields are masked by default (e.g., "••••••••••").
  - Include a "Reveal" button that will later require auth (for now, just toggles visibility by calling `get_credential`).
  - Include a "Copy" button next to password fields.
  - Show metadata: date added, last modified.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the detail view layout per credential type.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-012
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. The detail panel shows the selected credential's information.
Auth-gated reveal will be added in Phase 3 (TASK-018, TASK-019).

## Completion Criteria
- Detail view renders all fields per credential type.
- Passwords are masked by default.
- Reveal toggle works (calls `get_credential`).
- Copy button is present.
- Build succeeds.
