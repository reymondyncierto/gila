---
id: TASK-2026-04-04-016
title: Implement fuzzy search for credentials
domain: ui-ux-frontend
goal: >
  Add a search bar to the app layout that performs instant, fuzzy-search filtering
  of the credential list. Search queries are sent to the `search_credentials` Tauri
  command and results update the list in real-time.
scope_in:
  - src/components/layout/
  - src/components/credentials/
  - src/hooks/
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Search bar at the top of the sidebar or above the credential list.
  - Debounced input (300ms) to avoid excessive Tauri calls.
  - Call `search_credentials` Tauri command.
  - Show "No results" state.
  - Clear search button.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the search UX.
estimate_minutes: 20
dependencies:
  - TASK-2026-04-04-012
  - TASK-2026-04-04-008
design_revision: false
collaborators: []
status: approved
---

## Context
Phase 2 — Desktop Shell. Fuzzy search is a core vault operation from the PRD (section 4.3).
It uses the `search_credentials` Tauri command from TASK-008.

## Completion Criteria
- Search bar renders and accepts input.
- Results update with debouncing.
- No-results state is shown.
- Build succeeds.
