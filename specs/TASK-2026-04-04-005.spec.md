---
id: TASK-2026-04-04-005
title: Define encrypted SQLite schema and migrations for credential storage
domain: database
goal: >
  Design and implement the SQLite database schema for storing encrypted credentials.
  Create tables for all five credential types (Login, App Password, API Key, Wi-Fi,
  Secure Note), vault metadata, and the master password verification hash.
scope_in:
  - src-tauri/src/db/
  - src-tauri/migrations/
  - src-tauri/Cargo.toml
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Use `tauri-plugin-sql` with SQLite.
  - All credential field values must be stored encrypted (ciphertext blobs), not plaintext.
  - Store the Argon2id salt and a password verification token (encrypted known value) for master password validation.
  - Use a single `credentials` table with a `type` discriminator column and a JSON-structured encrypted `data` blob, or separate tables per type — choose the simpler approach.
  - Include `created_at`, `updated_at`, `favorite` columns.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
output:
  - List changed files.
  - Document the schema design.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-001
design_revision: false
collaborators: []
status: approved
---

## Context
Phase 2 — Desktop Shell. This establishes the persistent storage layer for the vault.
The crypto module (Phase 1) handles encryption; this module manages the storage of
encrypted data in SQLite.

## Completion Criteria
- Schema supports all five credential types.
- Migration runs successfully and creates all tables.
- Master password salt and verification token have dedicated storage.
