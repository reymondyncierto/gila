---
id: TASK-2026-04-04-024
title: Build auth prompt modal for per-action re-authentication
domain: ui-ux-frontend
goal: >
  Create a modal dialog that appears when a sensitive action (reveal, copy, edit, delete)
  requires re-authentication. The modal shows a password input and a biometric button
  placeholder. It integrates with the `request_auth`/`confirm_auth` flow.
scope_in:
  - src/components/auth/
  - src/hooks/
  - src/components/credentials/
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Modal overlay with password input.
  - Biometric button (placeholder for now).
  - On success, resolve a promise so the calling action can proceed.
  - On cancel, abort the action.
  - Wire into reveal, copy, edit, and delete actions.
  - Create a `useAuthGate` hook that wraps actions with the auth check.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the auth prompt UX flow.
estimate_minutes: 30
dependencies:
  - TASK-2026-04-04-022
  - TASK-2026-04-04-013
  - TASK-2026-04-04-015
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 3 — Biometric Gate. This modal is the frontend counterpart to the per-action auth
system (TASK-022). It gates all sensitive operations behind re-authentication.

## Completion Criteria
- Auth modal renders with password input.
- `useAuthGate` hook wraps sensitive actions.
- Successful auth allows the action to proceed.
- Cancellation aborts the action.
- Build succeeds.
