---
id: TASK-2026-04-04-022
title: Implement per-action re-authentication with 30-second grace period
domain: auth-domain
goal: >
  Create a Tauri command `request_auth` that sensitive frontend actions call before
  proceeding. If the user authenticated within the last 30 seconds, the request is
  automatically approved. Otherwise, it returns a "needs_auth" response so the frontend
  can show the auth prompt.
scope_in:
  - src-tauri/src/auth/
  - src-tauri/src/commands/
scope_out:
  - src/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Track the timestamp of the last successful authentication.
  - Grace period is 30 seconds (configurable constant).
  - `request_auth` returns `{ authorized: true }` or `{ authorized: false, reason: "needs_auth" }`.
  - `confirm_auth` accepts a master password, verifies it, and updates the last-auth timestamp.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
output:
  - List changed files.
  - Document the auth flow.
estimate_minutes: 20
dependencies:
  - TASK-2026-04-04-021
design_revision: false
collaborators: []
status: approved
---

## Context
Phase 3 — Biometric Gate. PRD section 4.4 requires per-action re-authentication with
a 30-second grace period. This is the backend logic; the frontend auth prompt is TASK-023.

## Completion Criteria
- `request_auth` correctly checks grace period.
- Within 30 seconds of last auth, returns authorized.
- After 30 seconds, returns needs_auth.
- `confirm_auth` verifies password and updates timestamp.
