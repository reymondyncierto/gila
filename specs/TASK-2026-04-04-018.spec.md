---
id: TASK-2026-04-04-018
title: Build password generator frontend UI
domain: ui-ux-frontend
goal: >
  Create a password generator UI component accessible from the create/edit credential
  form. Users can configure length, character sets, and toggle passphrase mode.
  Generated passwords can be copied or inserted directly into the form field.
scope_in:
  - src/components/generator/
  - src/components/forms/
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Slider or input for password length.
  - Checkboxes for character sets (uppercase, lowercase, digits, symbols).
  - Toggle for passphrase mode with word count control.
  - Live preview of generated password.
  - "Regenerate" button.
  - "Use Password" button to insert into the credential form field.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the generator UI.
estimate_minutes: 25
dependencies:
  - TASK-2026-04-04-017
  - TASK-2026-04-04-014
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. Frontend counterpart to the password generator backend (TASK-017).
Integrates into the credential create/edit form.

## Completion Criteria
- Generator UI renders with all controls.
- Calls `generate_password` Tauri command.
- Generated password can be inserted into form.
- Build succeeds.
