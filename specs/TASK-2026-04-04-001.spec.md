---
id: TASK-2026-04-04-001
title: Initialize Tauri v2 project with React, TypeScript, and Tailwind CSS
domain: rust-backend
goal: >
  Scaffold the Gila project as a Tauri v2 app with a React 19 + TypeScript frontend
  and Tailwind CSS styling. The project must compile successfully on both the Rust
  and frontend sides.
scope_in:
  - src-tauri/
  - src/
  - package.json
  - tsconfig.json
  - tailwind.config.js
  - vite.config.ts
  - index.html
scope_out:
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Use Tauri v2 (not v1).
  - Use React 19 with TypeScript.
  - Use Tailwind CSS for styling.
  - Use Vite as the bundler.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - cd src-tauri && cargo build
  - npm run build
output:
  - List changed files.
  - Summarize project structure.
estimate_minutes: 25
dependencies: []
design_revision: false
collaborators: []
status: in_progress
---

## Context
Phase 1 foundation. The project currently has no application code — only PRD, CLAUDE.md,
agent definitions, and spec templates. This spec bootstraps the entire Tauri v2 + React
project so all subsequent specs have a compilable base.

## Completion Criteria
- `npm run build` succeeds.
- `cd src-tauri && cargo build` succeeds.
- Tauri app launches with a blank React page.
