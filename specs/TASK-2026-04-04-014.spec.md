---
id: TASK-2026-04-04-014
title: Build create credential form with type-specific field templates
domain: ui-ux-frontend
goal: >
  Create a form component for adding new credentials. The user selects a credential
  type (Login, App Password, API Key, Wi-Fi, Secure Note) and the form dynamically
  renders the appropriate fields. On submit, calls the Tauri `create_credential` command.
scope_in:
  - src/components/credentials/
  - src/components/forms/
scope_out:
  - src-tauri/
  - specs/
  - .claude/
  - CLAUDE.md
constraints:
  - Dynamic form fields based on credential type selection.
  - Client-side validation (required fields, URL format, etc.).
  - Call `create_credential` Tauri command on submit.
  - Show success/error feedback.
  - Commit messages must use `type(scope): description` with type in `feat|fix|refactor|chore|docs|test|style`.
  - Never stage or commit orchestration artifacts (`specs/**`, `.claude/**`, `CLAUDE.md`).
  - After validation passes, commit immediately, then delete this spec file.
validation:
  - npm run build
output:
  - List changed files.
  - Describe the form schema per credential type.
estimate_minutes: 30
dependencies:
  - TASK-2026-04-04-012
  - TASK-2026-04-04-007
design_revision: true
collaborators:
  - ui-ux-frontend-specialist
status: approved
---

## Context
Phase 2 — Desktop Shell. This form is the primary way users add credentials to the vault.
It calls the `create_credential` Tauri command from TASK-007.

## Completion Criteria
- Type selector renders all five types.
- Form fields change dynamically per type.
- Validation prevents submission of incomplete data.
- Submit calls Tauri invoke.
- Build succeeds.
